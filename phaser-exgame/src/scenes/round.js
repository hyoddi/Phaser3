// 로딩화면 클래스


import Phaser from 'phaser';

export default class Loading extends Phaser.Scene{

    constructor(){
        super('round'); // 식별자를 round으로 설정
    }

    preload(){ //사전설정
    
    }

    create(){ //생성

    }
    
    update(){ //변경
        
    }

}
